common chained.from.root%     rem June	18,1979 -  T/F
common common.date$           rem yymmdd
common common.drive$          rem single alpha
common common.return.code%    rem 0 if ok, else 1,2,etc
common chained%               rem T/F
common common.msg$            rem passed beck to menu
common common.option%         rem passed to itself by menu driver
common common.serial.number$  rem serial number, stupid

